# Exercises

1. Describe an API response using `TypedDict` and validate fields with mypy.
2. Use `Literal` types to constrain acceptable configuration values.
3. Apply `Annotated` metadata and explain how tooling can consume it.
