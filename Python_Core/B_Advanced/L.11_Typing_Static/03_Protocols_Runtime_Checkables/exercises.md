# Exercises

1. Convert a duck-typed API to use a `Protocol` and enable runtime validation.
2. Demonstrate when `isinstance` works with protocols and when it fails.
3. Create a `Protocol` for asynchronous context managers.
