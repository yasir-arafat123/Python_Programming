# Exercises

1. Implement a generic cache wrapper using `TypeVar` for keys and values.
2. Create a `Protocol` that enforces an iterable contract and use it in a generic function.
3. Experiment with `TypeVar(bound=...)` and document how bounds change type inference.
