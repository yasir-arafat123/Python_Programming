# Exercises

1. Enable `warn_unused_ignores` and resolve the resulting warnings in a sample project.
2. Split configuration per module using `mypy` section overrides.
3. Document a rollout plan for increasing strictness incrementally.
