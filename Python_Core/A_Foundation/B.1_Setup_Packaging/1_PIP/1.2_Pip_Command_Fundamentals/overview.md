# pip Command Fundamentals

## Key points

- Common commands: `install`, `uninstall`, `list`, `show`, `freeze`, `check`.
- Using `-r` to install from requirements files; `--upgrade` and `--force-reinstall`.
- `--no-deps`, `--no-binary`, `--pre` and other flags.

## Notes

Add examples showing typical workflows and flags.