# pip Configuration Files

## Key points

- Locations: `pip.conf` (Linux), `pip.ini` (Windows), `~/.config/pip/pip.conf`.
- `pip` user config, site config, and environment variables that affect install behavior.
- Configure indexes, trusted hosts, and cache settings.

## Notes

Include examples for configuring private package indexes and credentials via `pip` configuration.