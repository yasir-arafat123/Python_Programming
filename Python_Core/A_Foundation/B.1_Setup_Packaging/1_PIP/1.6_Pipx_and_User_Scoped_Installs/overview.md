# pipx & User-scoped Installs

## Key points

- `pipx` for installing CLI apps in isolated environments.
- When to use `pipx` vs system `pip` or venvs.
- `--user` installs and PATH considerations on different OSes.

## Notes

Add examples for installing and running CLI tools with `pipx`.