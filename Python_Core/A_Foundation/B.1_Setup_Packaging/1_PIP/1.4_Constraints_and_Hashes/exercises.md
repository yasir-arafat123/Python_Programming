# Exercises

1. Create a `constraints.txt` that pins a conflicting transitive dependency and observe resolution.
2. Generate a hash-locked `requirements.txt` and install with `--require-hashes`. Note the workflow cost.