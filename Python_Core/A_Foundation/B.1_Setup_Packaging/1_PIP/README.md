# PIP

### Key things to learn

- Install/upgrade/uninstall packages
- `requirements.txt` & constraints files
- `pipx` vs `pip`; editable installs (`pip install -e .`)
- Freeze environments (`pip freeze > requirements.txt`)

### References

- Python roadmap — https://roadmap.sh/python

