# Exercises

1. Upgrade pip on your system and verify `pip --version`.
2. Install a package using `--user` and confirm that imports work in a new Python REPL.
3. Document differences between `pip` from OS package managers and `python -m pip`.