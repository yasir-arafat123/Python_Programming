# Exercises

1. Configure a project to create its virtual environment inside a local `.venv/` directory.
2. Update `.gitignore` to exclude `.venv/` and document why.
3. Compare the workflow to using globally shared environments.
