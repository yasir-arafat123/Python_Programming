# Exercises

1. Inspect `sys.path` at runtime and explain each entry's origin.
2. Add a temporary path for local imports using `importlib` utilities and clean it up afterward.
3. Diagnose an `ImportError` caused by an unexpected `sys.path` entry.
