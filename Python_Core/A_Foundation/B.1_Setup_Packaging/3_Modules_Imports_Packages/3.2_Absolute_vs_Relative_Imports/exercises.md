# Exercises

1. Refactor a module tree to use absolute imports and run the tests.
2. Convert absolute imports to explicit relative imports inside a package and note readability impacts.
3. Explain why implicit relative imports were removed in Python 3.
