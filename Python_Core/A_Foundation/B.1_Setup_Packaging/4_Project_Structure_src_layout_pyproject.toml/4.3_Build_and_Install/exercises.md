# Exercises

1. Build wheel and sdist artifacts for the sample package using `python -m build`.
2. Install the built wheel in a fresh virtual environment and smoke test the entry point.
3. Automate build + install in a simple PowerShell script.
