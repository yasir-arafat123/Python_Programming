# Exercises

1. Populate the `project` table of `pyproject.toml` with realistic metadata.
2. Add optional dependency groups and document their purposes.
3. Validate the metadata using `python -m build` or an external tool.
