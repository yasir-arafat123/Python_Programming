# Exercises

1. Convert an existing project from flat layout to `src/` layout and run its tests.
2. Demonstrate how the `src/` layout prevents accidental relative imports during development.
3. Document pros and cons of each layout for your team.
