# Project Structure (src layout, pyproject.toml)

### Key things to learn

- `src/` layout vs flat layout
- PEP 621 metadata in `pyproject.toml`
- Build/install with `python -m build`, `pip install .`

### References

- Python roadmap — https://roadmap.sh/python

