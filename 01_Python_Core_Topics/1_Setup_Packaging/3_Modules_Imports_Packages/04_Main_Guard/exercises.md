# Exercises

1. Convert a script to expose reusable functions and protect its runner with the main guard.
2. Demonstrate how `python -m package.module` interacts with the main guard.
3. Build a CLI entry point that delegates to a module function.
