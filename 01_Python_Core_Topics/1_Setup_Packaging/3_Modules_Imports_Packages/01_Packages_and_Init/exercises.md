# Exercises

1. Create a nested package structure and experiment with importing modules from it.
2. Remove `__init__.py` from a package and observe the effect.
3. Populate `__all__` in `__init__.py` to control re-exported names.
