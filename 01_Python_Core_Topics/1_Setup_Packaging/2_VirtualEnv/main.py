"""
Topic: 01_Setup & Packaging
Subtopic: VirtualEnv
Description: Create/activate virtual environments with venv.

TODO:
- Read the README.md in this folder.
- Run this file to see examples, then extend experiments.
"""
from __future__ import annotations
from typing import *
import sys, os, math, json, re, pathlib, itertools, collections, functools, logging, datetime
# Optional third-party (uncomment if installed)
# import numpy as np
# import pandas as pd
def demo():
    print("Create venv (shell): python -m venv .venv")
    print("Activate (Windows): .venv\Scripts\activate")
    print("Activate (Mac/Linux): source .venv/bin/activate")
if __name__ == "__main__":
    demo()
