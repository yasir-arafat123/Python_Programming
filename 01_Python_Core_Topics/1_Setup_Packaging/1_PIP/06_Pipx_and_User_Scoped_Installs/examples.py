"""Examples for pipx and user-scoped installs."""

# Install pipx
# python -m pip install --user pipx
# python -m pipx ensurepath

# Use pipx to install a CLI tool
# pipx install black

# TODO: document PATH steps for Windows and macOS.