# Exercises

1. Use `pip show` to locate package files on disk.
2. Create a `requirements.txt` via `pip freeze` and reinstall into a fresh venv.
3. Demonstrate `pip check` to find dependency conflicts.