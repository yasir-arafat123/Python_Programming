# Exercises

1. Annotate a function that computes the median of a list of numbers.
2. Add type hints to a dictionary-based configuration loader and run `mypy`.
3. Explain why annotations do not change runtime behavior by default.
