# Exercises

1. Use `pydantic` or `attrs` to validate input data for an API endpoint.
2. Build a decorator that enforces simple runtime type checks.
3. Compare runtime validation cost vs static checking benefits.
